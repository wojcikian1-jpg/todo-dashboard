# ──────────────────────────────────────────────────
# Three-environment setup
# ──────────────────────────────────────────────────
#
# 1. Local dev  → Docker Supabase (npx supabase start)
#    URL:  http://127.0.0.1:54321
#    Key:  printed by `supabase start` (anon key)
#
# 2. Preview    → dev Supabase project (Vercel preview deployments)
#    Set in Vercel env vars (Preview scope only)
#
# 3. Production → prod Supabase project (Vercel production deployment)
#    Set in Vercel env vars (Production scope only)
#
# For local development, fill in the values from `npx supabase start`:
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-local-anon-key>

# Optional: auto-sign-in with a shared demo account (skips login page)
# Create this user in your Supabase project's Auth dashboard first.
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=
